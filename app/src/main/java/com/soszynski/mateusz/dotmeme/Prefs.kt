package com.soszynski.mateusz.dotmeme

class Prefs {
    companion object {
        const val PREF_SCANNING_PAUSED = "pref.SCANNING_PAUSED"
        const val PREF_SCANNING_PAUSED_default = false
    }
}